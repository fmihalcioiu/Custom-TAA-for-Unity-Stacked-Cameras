# Custom-TAA-for-Unity-Stacked-Cameras
This is a custom TAA bypass that works with stacked cameras (base + overlay). It applies TAA on the base camera while ignoring overlay (UI, crosshairs etc.). Can be applied before or after post processing.
